# 面试模板扩展需求文档 v2.0

## 1. 需求背景

当前系统已有3套预置面试模板（项目经理、产品经理、销售人员），需要：
1. 扩展更多岗位的系统内置模板
2. 支持用户自定义创建/管理模板
3. 增加模板状态管理功能

## 2. 功能需求

### 2.1 新增系统内置模板（6个岗位）

| 序号 | 岗位名称 | 适用场景 |
|------|----------|----------|
| 1 | Java程序员 | 技术开发岗位 |
| 2 | 教育信息化产品经理 | 教育行业产品岗 |
| 3 | 售后服务 | 客户服务岗位 |
| 4 | 人力资源 | HR岗位 |
| 5 | 总经理助理 | 行政管理岗位 |
| 6 | AI应用实习生 | 实习生岗位 |

### 2.2 模板管理功能增强

#### 2.2.1 模板分类
- **系统模板**：预置的9套模板，不可删除，可修改状态
- **自定义模板**：用户创建的模板，可完全管理

#### 2.2.2 模板状态管理
- 新增 `isActive` 字段（布尔值）
- 状态：`可用` / `不可用`
- 系统模板和自定义模板均可切换状态

#### 2.2.3 模板CRUD操作

| 操作 | 系统模板 | 自定义模板 |
|------|----------|------------|
| 查看 | ✅ | ✅ |
| 创建 | ❌ | ✅ |
| 编辑 | ✅（仅问题内容） | ✅（全部字段） |
| 删除 | ❌ | ✅ |
| 状态切换 | ✅ | ✅ |

### 2.3 岗位管理集成

创建岗位时，模板选择器：
- 仅显示 `isActive = true` 的模板
- 按分类展示：系统模板 / 自定义模板
- 显示模板描述信息辅助选择

## 3. 数据模型变更

### 3.1 InterviewTemplate 表新增字段

```prisma
model InterviewTemplate {
  // ... 现有字段 ...

  isSystem    Boolean   @default(false)  // 是否系统模板
  isActive    Boolean   @default(true)   // 是否可用
  createdById String?                    // 创建者ID（自定义模板）
  createdBy   User?     @relation(fields: [createdById], references: [id])
}
```

### 3.2 User 表新增关联

```prisma
model User {
  // ... 现有字段 ...
  templates   InterviewTemplate[]  // 用户创建的模板
}
```

## 4. 新增模板详细设计

### 4.1 Java程序员

**评估维度**：技术能力、编码实践、系统设计、学习能力、职业动机

**示例问题**：
- 技术能力：介绍一下你最熟悉的Java项目？用了哪些技术栈？
- 编码实践：你们团队是怎么做代码review的？
- 系统设计：遇到过性能问题吗？怎么排查和优化的？
- 学习能力：最近在学什么新技术？为什么想学？
- 职业动机：为什么想换工作？期望的技术方向是什么？

### 4.2 教育信息化产品经理

**评估维度**：行业理解、产品能力、用户洞察、项目推进、职业动机

**示例问题**：
- 行业理解：你对教育信息化行业有什么了解？
- 产品能力：做过的教育类产品有哪些？核心功能是什么？
- 用户洞察：教育产品的用户群体有什么特点？
- 项目推进：教育产品的实施周期一般多长？遇到过什么困难？
- 职业动机：为什么选择教育行业？

### 4.3 售后服务

**评估维度**：服务意识、问题处理、沟通技巧、抗压能力、职业动机

**示例问题**：
- 服务意识：你觉得好的售后服务应该是什么样的？
- 问题处理：遇到过最棘手的客户投诉是什么？怎么解决的？
- 沟通技巧：客户情绪激动时，你一般怎么安抚？
- 抗压能力：工作中压力最大的时候是什么情况？
- 职业动机：为什么选择售后服务这个岗位？

### 4.4 人力资源

**评估维度**：专业知识、招聘能力、员工关系、数据分析、职业动机

**示例问题**：
- 专业知识：你最熟悉的HR模块是哪个？
- 招聘能力：招聘渠道你一般怎么选择？效果怎么评估？
- 员工关系：处理过员工纠纷吗？怎么处理的？
- 数据分析：你们公司的离职率是多少？主要原因是什么？
- 职业动机：HR工作中你最有成就感的事是什么？

### 4.5 总经理助理

**评估维度**：综合能力、协调沟通、文书写作、保密意识、职业动机

**示例问题**：
- 综合能力：日常工作主要负责哪些事务？
- 协调沟通：怎么协调多个部门的会议安排？
- 文书写作：写过什么类型的公文或报告？
- 保密意识：工作中接触过敏感信息吗？怎么处理的？
- 职业动机：你觉得这个岗位最重要的能力是什么？

### 4.6 AI应用实习生

**评估维度**：基础知识、学习能力、实践经验、团队协作、职业规划

**示例问题**：
- 基础知识：了解哪些AI相关的技术或工具？
- 学习能力：平时怎么学习AI相关知识的？
- 实践经验：做过AI相关的项目或作业吗？
- 团队协作：在学校参加过什么团队项目？
- 职业规划：实习期望学到什么？未来想往哪个方向发展？

## 5. UI/UX 设计要点

### 5.1 模板列表页改造

```
┌─────────────────────────────────────────────────────┐
│  面试模板管理                    [+ 新建自定义模板]  │
├─────────────────────────────────────────────────────┤
│  筛选：[全部 ▼] [可用 ▼] [系统/自定义 ▼]           │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐   │
│  │ 📋 项目经理          [系统] [可用]          │   │
│  │ 适用于项目经理岗位的面试模板                │   │
│  │ 5-10题 | 5个维度                            │   │
│  │                    [编辑] [切换状态]        │   │
│  └─────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────┐   │
│  │ 📋 我的销售模板       [自定义] [可用]       │   │
│  │ 针对B2B销售的定制模板                       │   │
│  │ 6-8题 | 4个维度                             │   │
│  │                [编辑] [切换状态] [删除]     │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 5.2 新建/编辑模板表单

- 模板名称（必填）
- 模板描述（选填）
- 最少/最多问题数
- 评估维度（可增删）
- 各维度示例问题（可增删改）

### 5.3 岗位创建时的模板选择

```
┌─────────────────────────────────────────────────────┐
│  选择面试模板                                       │
├─────────────────────────────────────────────────────┤
│  系统模板                                           │
│  ○ 项目经理 - 适用于项目经理岗位                   │
│  ○ 产品经理 - 适用于产品经理岗位                   │
│  ○ Java程序员 - 适用于技术开发岗位                 │
│  ...                                                │
├─────────────────────────────────────────────────────┤
│  自定义模板                                         │
│  ○ 我的销售模板 - 针对B2B销售的定制模板            │
│  ○ 运营专员 - 适用于运营岗位                       │
└─────────────────────────────────────────────────────┘
```

## 6. API 设计

### 6.1 模板相关 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/templates | 获取模板列表（支持筛选） |
| POST | /api/templates | 创建自定义模板 |
| GET | /api/templates/:id | 获取模板详情 |
| PUT | /api/templates/:id | 更新模板 |
| DELETE | /api/templates/:id | 删除自定义模板 |
| PATCH | /api/templates/:id/status | 切换模板状态 |

### 6.2 查询参数

```typescript
interface TemplateQueryParams {
  isSystem?: boolean    // 筛选系统/自定义
  isActive?: boolean    // 筛选可用状态
  search?: string       // 搜索名称
}
```

## 7. 实施计划

### 阶段一：数据模型升级
1. 修改 Prisma Schema
2. 创建数据库迁移
3. 更新种子数据（新增6个系统模板）

### 阶段二：后端 API 开发
1. 扩展模板 CRUD API
2. 新增状态切换 API
3. 更新岗位创建时的模板查询

### 阶段三：前端页面开发
1. 改造模板列表页
2. 新建/编辑模板表单
3. 更新岗位创建页的模板选择器

### 阶段四：测试与部署
1. 功能测试
2. 数据迁移验证
3. 部署上线

## 8. 已确认事项

1. **系统模板编辑权限**：✅ 允许修改问题内容（`questionTemplates`字段）
2. **模板删除策略**：✅ 已被岗位使用的自定义模板不能删除
3. **模板复制功能**：✅ 支持从现有模板复制创建新模板
4. **各岗位评估维度**：✅ 确认无需调整
5. **问题数量配置**：✅ 统一为5-10题

## 9. 补充设计

### 9.1 模板复制 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/templates/:id/copy | 复制模板 |

**复制逻辑**：
- 新名称：`{原名称}-副本`
- `isSystem`：false（复制后变为自定义模板）
- `isActive`：true
- `createdById`：当前用户ID

### 9.2 删除校验逻辑

```typescript
// 删除前检查
const positionCount = await prisma.position.count({
  where: { templateId: id }
})
if (positionCount > 0) {
  return { error: '该模板已被岗位使用，无法删除' }
}
```

### 9.3 系统模板可编辑字段

| 字段 | 系统模板 | 自定义模板 |
|------|----------|------------|
| name | ❌ | ✅ |
| description | ❌ | ✅ |
| minQuestions | ❌ | ✅ |
| maxQuestions | ❌ | ✅ |
| dimensions | ❌ | ✅ |
| questionTemplates | ✅ | ✅ |
| isActive | ✅ | ✅ |

---

**文档版本**：v2.1
**创建日期**：2025-12-03
**状态**：已确认，开发中
